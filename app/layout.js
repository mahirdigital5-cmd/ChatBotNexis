export const metadata = {
  title: "ChatBotNexis",
  description: "WA Auto Reply Bot",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
